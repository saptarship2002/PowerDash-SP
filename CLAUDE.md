# Working preferences

- Do not start the dev server (`npm run dev`) on your own initiative to verify changes. Only run it when the user explicitly asks you to run/test the app. Prefer type-checking (`npx tsc --noEmit`) and reading the diff to confirm correctness otherwise.
